<Page
    x:Class="AirportUWPClient.Views.PilotView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:AirportUWPClient.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}"
    DataContext="{Binding PilotVMInstance, Source={StaticResource Locator}}">

    <StackPanel>
        <CheckBox Content="CheckBox" HorizontalAlignment="Left" Margin="783,452,0,0" VerticalAlignment="Top"/>
        <Button Content="Pilots" Command="{Binding ShowPilotsCommand}"></Button>
        <Button Content="Pilots" Command="{Binding ShowPilotCommand}"></Button>

    </StackPanel>
</Page>
